# 统一按 LF 入库、按 LF 检出。
#
# 项目只在 Linux 上开发运行，但仓库可能被在 Windows 上 clone 出来看一眼 ——
# 那边 git 默认把文本文件检出成 CRLF，而 build-aux/*.sh、packaging/render-icons.sh
# 这类脚本一旦带上 CRLF 就直接无法执行（bad interpreter: 找不到 \r）。
# 统一成 LF 之后，Dart/Zig/YAML 也不再有"整文件只有行尾不同"的无意义 diff。
* text=auto eol=lf

# 位图不做行尾转换。text=auto 下它们本来就会被认成二进制，写明了是防误判
*.png binary
